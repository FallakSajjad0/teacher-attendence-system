const A=require('../models/Attendance');exports.recordAvailabilityResponse=async(id,userId,isAvailable)=>{const a=await A.byId(id);if(!a||a.user_id!==userId)throw Error('Attendance record not found');return A.update(id,{status:isAvailable?'available':'not_available',availability_response_at:new Date()})};exports.confirmArrival=async(id,userId,location)=>{const a=await A.byId(id);if(!a||a.user_id!==userId)throw Error('Attendance record not found');return A.update(id,{status:'arrived',arrival_confirmed_at:new Date(),latitude:location.latitude,longitude:location.longitude})};exports.getMyAttendance=id=>A.byTeacher(id);