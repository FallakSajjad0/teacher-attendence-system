const r=require('express').Router(),c=require('../controllers/classController'),{authenticate}=require('../middleware/auth'),{isAdmin}=require('../middleware/roleCheck'),v=require('../middleware/validation');r.use(authenticate);r.get('/today',c.getTodayClasses);r.get('/',c.getClasses);r.post('/',isAdmin,v.classValidation,c.createClass);r.get('/:id',c.getClassById);r.put('/:id',isAdmin,c.updateClass);r.delete('/:id',isAdmin,c.deleteClass);module.exports=r;