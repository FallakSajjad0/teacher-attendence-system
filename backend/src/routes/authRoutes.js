const r=require('express').Router(),c=require('../controllers/authController'),{authenticate}=require('../middleware/auth'),{isAdmin}=require('../middleware/roleCheck'),v=require('../middleware/validation');r.post('/register',authenticate,isAdmin,v.registerValidation,c.register);r.post('/login',v.loginValidation,c.login);r.get('/me',authenticate,c.getMe);r.put('/fcm-token',authenticate,c.updateFcmToken);r.put('/change-password',authenticate,c.changePassword);module.exports=r;