const r=require('express').Router(),c=require('../controllers/userController'),{authenticate}=require('../middleware/auth'),{isAdmin,isManagement}=require('../middleware/roleCheck');r.use(authenticate);r.get('/',isManagement,c.getUsers);r.get('/teachers',isManagement,c.getTeachers);r.get('/:id',isManagement,c.getUserById);r.put('/:id',isAdmin,c.updateUser);r.delete('/:id',isAdmin,c.deleteUser);module.exports=r;