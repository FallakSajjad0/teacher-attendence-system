import 'package:flutter/material.dart';
class SystemConfig extends StatelessWidget { const SystemConfig({super.key}); @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('System Configuration')),body:const Center(child:Text('System configuration'))); }
