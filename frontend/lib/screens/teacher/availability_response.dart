// Placeholder - availability is handled inline in ClassDetailScreen
// This file can be used for a dedicated availability screen if needed.
import 'package:flutter/material.dart';

class AvailabilityResponseScreen extends StatelessWidget {
  const AvailabilityResponseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Availability response handled in class detail')),
    );
  }
}